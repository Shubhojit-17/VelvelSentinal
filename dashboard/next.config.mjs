/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Standalone output disabled for Windows dev - enable for Docker
  // output: 'standalone',
};

export default nextConfig;
